/*let Persona = function(fName, lName){ //Creo el objeto
  this.firstName = fName; //Atributo publico
  this.lastName = lName;
  let edad = 25; //Atributo privado
  
  this.getFullName = function() //Metodo
  {
    return `${this.firstName} ${this.lastName}`;
  }
  
  this.getEdad = function()
  {
    return edad;
  }

}

let persona1 = new Persona('Franco', 'Cordoba'); //Creo la instancia del objeto
let persona1FullName = persona1.getFullName(); //Accedo al metodo
let persona1Edad = persona1.getEdad(); //Accedo al metodo que me devuelve un atributo privado

console.log(persona1FullName); //Imprimo a traves de metodo

console.log(persona1Edad);

console.log(persona1.firstName); //Imprimo directamente un atributo publico */


/* CODIGO DEWL PROF
let Persona = function(firstName, lastname, date) {
  this.firstName = firstName;
  this.lastname = lastname;
  this.date = date;
  this.getFullName = function () {
    return `${this.firstName} ${this.lastname}`
  }
  let calculateAge1 = () => {
    let ageDifMs = Date.now() - new Date(this.date);
    let ageDate = new Date(ageDifMs);
    return Math.abs(ageDate.getUTCFullYear() - 1970);
  }
  this.getAge = function () {
    return calculateAge();
  }
}
let persona = new Persona('Jose', 'San Martín', '1740-03-25');
console.log(persona.getAge());*/


/*let Usuario = function(id, nombre, apellido, score)
{
  this.id = id;
  this.nombre = nombre;
  this.apellido = apellido;
  this.score = score;
  
  this.getScore = function()
  {
    return score;
  }
  
  this.getFullName = function(){
    
  }
}

let Game = function(id)
{
  this.id = id;
  this.score = 0;
  this.sumScore= function(newValue)
  {
    this.score = this.score + newValue;
  }
  
  this.getScore = function()
  {
    return this.score;
  }
  
  this.saveGame = function()
  {
    //Guardar puntaje en DB
  }
}

let user = new Usuario(34523, 'Jugador', '2', 100);

let fullName = user.getFullName();

let game = new Game(user.id);

game.sumScore(50);
game.saveGame();
*/
